import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.adisney.chorephoto',
  appName: 'chore-photos',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
